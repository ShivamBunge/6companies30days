def findTwoElement( self,arr, n): 
        dct = {}
        a,b = 0,0
        for i in range(1,n+1):
            dct[i] = 0
        for ele in arr:
            dct[ele] = dct.get(ele,0) + 1
        for key in dct:
            if dct[key] == 0:
                a = key
            elif dct[key] > 1:
                b = key
        return b,a